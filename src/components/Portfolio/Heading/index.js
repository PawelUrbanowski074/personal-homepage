import { GitHubIcon, Paragraph, Header, Wrapper } from "./styled";

const Heading = () => (
  <Wrapper>
    <GitHubIcon />
    <Header> Portfolio </Header>
    <Paragraph>My recent projects</Paragraph>
  </Wrapper>
);

export default Heading;